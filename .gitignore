# dependencies
node_modules/

# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Native
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Metro
.metro-health-check*

# Env / secrets
# .env 는 커밋함 — EXPO_PUBLIC_* 클라이언트 공개키만 담는다 (eas update 번들에 필요).
# 진짜 비밀값은 .env.local 에 (커밋 안 됨).
.env.local
.env*.local

# Debug
npm-debug.*
yarn-debug.*
yarn-error.*

# macOS / Windows
.DS_Store
Thumbs.db
